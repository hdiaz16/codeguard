# Fixture de semgrep --test — no es código del producto.
jobs:
  compilar:
    runs-on: ubuntu-latest
    steps:
      # ruleid: gha-action-sin-sha
      - uses: actions/checkout@v4
      # ok: gha-action-sin-sha
      - uses: actions/checkout@0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b
      # ok: gha-action-sin-sha
      - uses: ./.github/actions/preparar-entorno
