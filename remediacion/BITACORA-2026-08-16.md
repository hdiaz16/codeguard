# Bitácora 2026-08-16/17 — EMPEZAR AQUÍ al retomar (vale en cualquier PC)

Esta bitácora viaja con el repo a propósito: la memoria del agente es local a
cada máquina, y el trabajo sigue mañana en otra. Todo lo de abajo está
**commiteado y subido** hasta `b48fb05` + esta bitácora.

## Estado al cierre

- **Versión: 1.14.0 instalada por ciclo formal** (la 1.13.2 fue la validada
  de punta a punta sobre instalación limpia; la 1.14.0 añade encima la
  ventisca v2 del sitio y el panel con semáforo — ver commit `a29c1ce`).
  En el ciclo 1.14.0 el desinstalador paró el daemon POR LAS BUENAS
  (daemon-stop, sin aviso de fuerza): cero orbes fantasma.
- **El sitio está DESPLEGADO en Vercel** y `codeguard.com.mx` sirve el build
  nuevo (verificado: el bundle publicado contiene el shader de la ventisca).
  Redespliegue: `vercel deploy --prod --yes` desde `sitio/`. URL pública de
  respaldo: `https://codeguard-three.vercel.app/`.
- **Iteración de diseño de la noche del 16 (todo commiteado):** el sitio
  terminó en EL MAR DE NUBES (mont-fort adaptado: volumen por deformación de
  dominio + autosombra + silver lining, claro compositivo, grano y viñeta,
  travelling de acercamiento con el scroll, vientos en calma; la nieve de
  puntos se ELIMINÓ — los copos estirados se veían rectangulares, lección:
  estelas de verdad exigen quads). El panel terminó LIMPIO: paleta MONTAÑA
  unificada en las 5 ventanas (config/explorer/widget hablaban 3 dialectos),
  glifo de estado junto al veredicto de 24px, y NADA de acento de estado en
  la tarjeta (dos intentos rechazados por Héctor: ni línea ni brillo — la
  tarjeta es el escenario, no el actor). El daemon corre un binario preview
  `1.14.0+ui2`; FALTA el ciclo formal 1.14.1 con su visto bueno.
- **PENDIENTE DE VISTO BUENO DE HÉCTOR** (no lo dio antes del cierre):
  1. La **ventisca del sitio** en vivo: el descenso por los climas
     (`sitio/src/js/ambiente3d.js`). Los mandos son constantes con nombre en
     el vertex shader: la curva del guion (8%→38% sube, 62%→92% amaina), el
     viento, la densidad y el brillo.
  2. El **panel nuevo** del daemon: veredicto 24px CON GLIFO de estado
     (escudo/octágono/círculo girando) y el SEMÁFORO de la tarjeta (línea de
     acento con brillo en el borde superior, color semántico). Ya instalado
     por ciclo formal 1.14.0.
  3. La **ventisca v2**: tres bandas de profundidad (polvo/copos/BOKEH),
     estelas de viento, gradación de color por clima. Desplegada en Vercel.
     Mando de afinación: `?clima=0.5` en la URL fija el punto del guion.

## Lo cerrado el 2026-08-16 (todo con validador independiente y/o mutación)

| qué | commit | prueba |
|---|---|---|
| 5 fugas de la compuerta de secretos (`.gitattributes -diff`, NUL, allowlist, `.gitleaksignore`, `# gitleaks:allow`) | `53f988f` | 6 mutaciones exactas + 11/11 E2E en instalación limpia |
| CI salía 0 con capas apagadas (`pipeline.SinGarantia`) | `8df4137` | 7/7 E2E medidos (A=1,B=1,C=1,D=0, shadow/sin-config/diff-grande=0) |
| Etapa de secretos sin plazo (60 s fail-closed + guarda AST) | `8df4137` | mutación roja en la línea exacta |
| Aviso índice-vs-disco (`git add -p`) — sólo avisa, el fondo es decisión de arquitectura | `8df4137` | E2E por el gancho |
| `go vet -json` es un FLUJO: govet no analizaba nada en repos con test externo | `902670f` | mutación reproduce el error exacto |
| Informe: «resueltos» falsos con capa caída | `257db21` | 3 corridas reales renombrando staticcheck |
| Orbe fantasma: apagado IPC `daemon-stop` + uninstall pide antes de matar + limpia entrada Inno | `8a7fd9d` | daemon-stop medido en 0,3 s |
| Carrera del desinstalador gráfico («Removed all? No» con exit 0, 23,7 MB huérfanos) | `653ebb5` | re-validado en 2 ciclos limpios («Removed all? Yes») |
| 5 CVEs alcanzables (x/text 0.41.0 + toolchain go1.26.6) | `7996668` | govulncheck: «No vulnerabilities found» |
| Panel: veredicto 24px, escala --fs-*, primer-uso, sparkline; + 2 SVG destruidos por texto/emoji | `22d8540` | build + parseo; visual pendiente del ciclo 1.14.0 |
| Sitio: orbe ORIGINAL restaurado (decisión de Héctor: la insignia no se sustituye), acordeón de motores (~4400px→~1150px), principios en tira, −⅓ scroll | `a8bb2b7`+`75d61ec` | capturas reales con Edge headless |
| Sitio: el descenso por los climas — ventisca de 1400 partículas con profundidad conducida por el scroll, niebla al 0.055 (nivel de body::before), todo premultiplicado (quemado imposible por driver) | `29c2189`..`b100caa` | calma verificada con captura + luminancia idéntica a producción; la ventisca a mitad de scroll sólo se juzga en vivo |

**Validación completa sobre instalación limpia (5 validadores): 0 fallos.**
Suite: verdes salvo los 7 de entorno conocidos (javafmt ×5 por JDK17 vs jar 21,
mypy ×2 por alias 8.3 de TEMP).

## Decisiones de producto tomadas (no reabrir sin motivo)

- **gitleaks y la entropía**: un `ghp_` con relleno secuencial NO se detecta
  (umbral de entropía del upstream). Héctor decidió DEJARLO así para evitar
  falsos positivos en docs. Mejora opcional anotada: regla propia sin entropía
  como AVISO no bloqueante en la 2ª pasada neutral (~medio día).
- **`# gitleaks:allow` ya no silencia** (consecuencia de la unión de las dos
  pasadas). Fixtures: partir el literal (ver `patDePrueba`).
- **El orbe del héroe del sitio es el ORIGINAL** (gooey CSS/SVG). No 3D. La
  dimensión 3D del sitio vive en la atmósfera.
- La huella `ts-innerhtml-var` COLAPSA (regla+archivo): una línea de baseline
  cubre los 4 usos escapados de `main.js`. Ver el pendiente de
  ComputeFingerprint abajo.

## LA CAPA LLM QUEDÓ SÓLIDA (2026-08-17 madrugada, commit `ec079f7`)

Decisión de Héctor: capa de consejo firme ANTES de encenderla. Los CINCO
defectos LLM cerrados de raíz con dos agentes Opus + mutaciones verificadas
de forma independiente por el orquestador:
- **[14]** la sombra ya no apuesta: `esperarRunPersistido` (backoff, tope 30 s)
  + `UpdateRunLLM` con `RowsAffected` y `ErrRunNoExiste` — cero filas ya no es
  silencio.
- **[18]** `ON CONFLICT DO UPDATE` de sólo risk_score/llm_used en runs +
  `reencolarRunParaCentral` (la marca de agua retrocede para que el run
  corregido VUELVA a viajar).
- **[23]** `decidirClave` pura: bóveda rota ≠ clave ausente; el fallo se dice
  en voz alta sin inundar el log; la caída al entorno se conserva (P2).
- **[24]** fuera el `http.Client.Timeout` fijo de 3 min (techo escondido que
  cortaba streams largos); el context de cada llamada manda — verificado en
  los 4 caminos.
- **[25]** `Load` sanea `max_diff_tokens <= 0` al default; la plantilla de
  init ya no invita al error.
- **(primo del [25], destapado por el reporte del agente)** `timeout_ms: 0`
  creaba un context nacido vencido y la explicación del panel fallaba al
  instante — mismo clamp en Load (`b48fb05`).
Para ENCENDERLA: configurar clave desde el panel (botón del modelo) o
`install.ps1 -ApiKey`. **Rotar las claves sigue pendiente (sólo Héctor).**

**Notas de los reportes de los agentes (2026-08-17), para quien retome:**
- El arreglo de [18] necesitó DOS piezas y está medido: el `DO UPDATE` solo
  dejaba el test en rojo — sin el retroceso de marca la fila corregida jamás
  volvía a pasar por el INSERT.
- `reencolarRunParaCentral` es **best-effort**: un empuje corriendo justo en
  ese instante puede pisarle el retroceso (queda como estaba antes de existir
  la función, nunca peor). Y ESCRIBE en `sync_marcas` — quien haga [20] debe
  mirarla.
- El `DO UPDATE ... excluded` NO está medido contra Postgres real (falta
  `CODEGUARD_TEST_PG_DSN`; en SQLite sí). Para cerrarlo:
  `docker run -d -e POSTGRES_PASSWORD=test -p 55432:5432 postgres:16-alpine`.
- `risk_score` NO tiene ningún lector en el producto salvo el sync (ni panel,
  ni CLI, ni stats) — por eso su pérdida era invisible. Dato para priorizar.
- En shadow.go quedan `_ = SaveLLMCall(...)` y afines (3 sitios de la misma
  familia que [14]): anotados, no arreglados.
- Si el hook muere entre responder y persistir (Ctrl-C), la sombra de ese
  commit se pierde a cambio de no quemar tokens sin dónde guardarlos —
  decisión deliberada, reversible en una línea.

## PENDIENTES, por orden de valor

1. **[20] sync.go: un `feedback` huérfano mata el sync de esa máquina PARA
   SIEMPRE** — la marca de agua no avanza y el lote entero (hasta 500 filas)
   cae en una sola transacción: una fila mala bloquea 499 sanas. El propio
   comentario de sync.go admite el disparador (ULID del mismo milisegundo).
   **EL PRIMERO DE MAÑANA — decisión explícita de Héctor.** Ojo: [18] añadió
   `reencolarRunParaCentral` que RETROCEDE la marca de runs a propósito — al
   arreglar [20] no confundir ese retroceso deliberado con el bug.
2. **[21] `runTool` plano devuelve éxito con ExitError y salida vacía** —
   `ruff format --check` sin red. La clase del contrato de motores, sin cerrar ahí.
3. **[13] ProtocolVersion se estampa y nunca se compara** (muerde al
   actualizar con el daemon vivo).
4. **[17] dos clics rápidos → el explorador muestra el grafo/overlay del repo
   EQUIVOCADO** (falta token de generación en prepararGrafo).
5. **[31] enrolamiento parcial sin salida**: `init` dice «ya enrolado» con
   hooks sin cablear (le pasó a Héctor 2 veces tras reinstalar; `status`
   sí lo caza, `install` lo arregla).
6. **[07]/[08] carreras de migración** SQLite/Postgres (DDL sin IF NOT
    EXISTS, sin lock). `-race` NO las ve: son inter-proceso.
7. **[32] CostoMicros**: `&&` donde va `||` — tarifas parciales subestiman gasto.
8. **[22] excepciones de identidad sin guarda en init()** (datos hoy limpios).
9. **[28]/[29] grafo**: IDs SQL divergentes Go/TS; `codeguard graph` falla
    entero con go.mod+main.go en raíz (el layout más común).

**Nuevos de hoy (añadir al triaje):**
- **El orbe no se repinta si Explorer se reinicia** (no escucha TaskbarCreated
  — proceso vivo, icono ausente). Lado Wails del daemon.
- **La caché sirvió resultados con línea DESACTUALIZADA** tras un cambio de
  solo-comentario (~55 ms uniformes en todos los motores; lo cazó un validador).
  Mecanismo sin entender — investigar antes de arreglar.
- **Cosmético CI**: imprime «OK — 0 bloqueantes» justo antes de «NO PUEDO
  GARANTIZAR» (printSummary corre antes del veredicto de garantía).
- **Cosmético status**: «✗ baseline» cuando el archivo existe con 0 supresiones.
- **La huella que colapsa** (decisión de Héctor pendiente desde el 15): también
  afecta a semgrep, no sólo a govet/staticcheck/tsc. Arreglarla invalida
  baselines de repos enrolados.

**Infraestructura (decisiones de Héctor pendientes):**
- **En CI no corre NI UN go test** (los 2 workflows solo hacen build+motores+shadow).
- **`-race`**: en esta familia de máquinas no puede correr (sin gcc); el camino
  Linux está bloqueado POR CÓDIGO (internal/ipc usa winio sin build tags: 5
  paquetes no compilan fuera de Windows). Vía viable: `windows-latest` (gcc de
  Strawberry Perl; alta confianza, sin confirmar en runner real). Job de humo
  `continue-on-error` primero. Los 14 hallazgos de concurrencia siguen
  congelados hasta esto.
- **Rotar `ANTHROPIC_API_KEY` y `FOUNDRY_API_KEY`** (desde el 14; sólo Héctor).
- JDK 21 para google-java-format (hoy degradado con JDK 17).
- Silent install del Setup NO arranca el daemon (`skipifsilent`) — ¿correcto
  para reparto masivo? Decisión pendiente.

## Trampas de método que ya cobraron (no repetir)

- El gancho solo corre la etapa 1; **la etapa 2 la corre el DAEMON**: para
  medir un cambio de motor hay que REINSTALAR (o test de integración sin daemon).
- `codeguard baseline` regenera contra TODO el repo: casi mete 124 supresiones
  nuevas incluida una CVE alcanzable. Anexar huellas quirúrgicamente.
- El fixture de repo de juguete necesita lenguaje soportado COMMITEADO o init
  se niega (y el gancho no se instala → control falso). Hooks en `.githooks/`.
- La clave de ejemplo de AWS (AKIA...EXAMPLE) está en el allowlist de gitleaks:
  invalida controles. PAT sintético partido en dos.
- `codeguard ci` analiza el WORKING TREE: `git checkout <head>` antes de cada corrida.
- Medir la forma de salida de una herramienta EN EL CASO REAL (go vet -json es
  flujo, no objeto; se midió con un paquete y costó semanas de govet ciego).
